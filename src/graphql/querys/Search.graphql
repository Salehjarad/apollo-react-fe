query search($query: String) {
  search(query: $query) {
    id
    content
    doc_date
    doc_type
    doc_number
    file_url
    user {
      id
      username
    }
    tags {
      id
      value
    }
  }
}
